import
	..Type.Method

+. new Method
	| Combines two values of the same type into a greater value of that type.
	name.
	args. 2

-. new Method
	| `+ b res` should be `a`.
	name.
	args. 2

*. new Method
	| Sum of `b` copies of `a`.
	name.
	args. 2

/. new Method
	| `* b res` should be `a`.
	name.
	args. 2
