use
	..Type.Method

+. Method
	doc.
		"Combines two values of the same type into a greater value of that type."
	args. 2

-. Method
	doc.
		"`+ b res` should be `a`."
	args. 2

*. Method
	doc.
		"Sum of `b` copies of `a`."
	args. 2

/. Method
	doc.
		"`* b res` should be `a`."
	args. 2
