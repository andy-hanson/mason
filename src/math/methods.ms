use
	..Type.Method

+. new Method
	name.
	doc.
		"Combines two values of the same type into a greater value of that type."
	args. 2

-. new Method
	name.
	doc.
		"`+ b res` should be `a`."
	args. 2

*. new Method
	name.
	doc.
		"Sum of `b` copies of `a`."
	args. 2

/. new Method
	name.
	doc.
		"`* b res` should be `a`."
	args. 2
