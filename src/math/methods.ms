use
	..Type.Method

+. Method
	doc. |a b
		"Combines two values of the same type into a greater value of that type."

-. Method
	doc. |a b
		"`+ b res` should be `a`."

*. Method
	doc. |a b
		"Sum of `b` copies of `a`."

/. Method
	doc. |a b
		"`* b res` should be `a`."
